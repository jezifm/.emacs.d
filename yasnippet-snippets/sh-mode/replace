# -*- mode: snippet -*-
# name: replace_in_file
# key: replace
# --
function replace_in_file {
    if [ "\$OS" = 'Darwin' ]; then
        # for MacOS
        sed -i '' -e "\$1" "\$2"
    else
        # for Linux and Windows
        sed -i'' -e "\$1" "\$2"
    fi
}