# -*- mode: snippet -*-
# name: createtemp
# key: createtemp
# key: ctt
# --
drop table if exists
    tmp_`(jez-abbreviate (buffer-name (current-buffer)))`_$1
;

create temp table
    tmp_`(jez-abbreviate (buffer-name (current-buffer)))`_$1
as (
    select
        $0
    from
        $2
);